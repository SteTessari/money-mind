spring:
  datasource:
    url: jdbc:mysql://localhost:3306/money-mind
    username: root
    password: sft6033
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
api:
  security:
    token:
      secret: my-secret-key


